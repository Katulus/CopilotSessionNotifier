<Window x:Class="CopilotNotifier.UI.NotificationPopup"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ShowActivated="False"
        Width="360"
        SizeToContent="Height"
        ResizeMode="NoResize">
    <Window.Resources>
        <Storyboard x:Key="SlideIn">
            <DoubleAnimation Storyboard.TargetProperty="(Window.Opacity)"
                             From="0" To="1" Duration="0:0:0.3"/>
            <ThicknessAnimation Storyboard.TargetName="RootBorder"
                                Storyboard.TargetProperty="Margin"
                                From="20,10,0,10" To="0,10,0,10"
                                Duration="0:0:0.3">
                <ThicknessAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </ThicknessAnimation.EasingFunction>
            </ThicknessAnimation>
        </Storyboard>
        <Storyboard x:Key="SlideOut" Completed="SlideOut_Completed">
            <DoubleAnimation Storyboard.TargetProperty="(Window.Opacity)"
                             To="0" Duration="0:0:0.2"/>
        </Storyboard>
    </Window.Resources>

    <Border x:Name="RootBorder"
            Background="{DynamicResource PopupBackground}"
            BorderBrush="{DynamicResource PopupBorderBrush}"
            BorderThickness="1"
            CornerRadius="10"
            Margin="0,10,0,10"
            Cursor="Hand"
            MouseLeftButtonUp="OnBodyClick">
        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="20" Opacity="0.5" ShadowDepth="3"/>
        </Border.Effect>

        <Grid Margin="16,12,12,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Icon -->
            <TextBlock x:Name="IconText"
                       Grid.Column="0" Grid.Row="0" Grid.RowSpan="2"
                       FontSize="28"
                       VerticalAlignment="Center"
                       Margin="0,0,12,0"/>

            <!-- Title -->
            <TextBlock x:Name="TitleText"
                       Grid.Column="1" Grid.Row="0"
                       Foreground="{DynamicResource TitleForeground}"
                       FontSize="14"
                       FontWeight="SemiBold"
                       TextTrimming="CharacterEllipsis"
                       MaxWidth="240"/>

            <!-- Message -->
            <TextBlock x:Name="MessageText"
                       Grid.Column="1" Grid.Row="1"
                       Foreground="{DynamicResource MessageForeground}"
                       FontSize="12"
                       Margin="0,2,0,0"
                       TextTrimming="CharacterEllipsis"
                       MaxWidth="240"/>

            <!-- Time -->
            <TextBlock x:Name="TimeText"
                       Grid.Column="1" Grid.Row="2"
                       Foreground="{DynamicResource TimeForeground}"
                       FontSize="10"
                       Margin="0,4,0,0"/>

            <!-- Close button -->
            <Button Grid.Column="2" Grid.Row="0"
                    Content="✕"
                    FontSize="14"
                    Foreground="{DynamicResource CloseButtonForeground}"
                    Background="Transparent"
                    BorderBrush="Transparent"
                    Cursor="Hand"
                    Click="OnCloseClick"
                    VerticalAlignment="Top"
                    Padding="4,0,0,0">
                <Button.Style>
                    <Style TargetType="Button">
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="{DynamicResource CloseButtonHoverForeground}"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </Grid>
    </Border>
</Window>
